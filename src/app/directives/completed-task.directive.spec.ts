import { CompletedTaskDirective } from './completed-task.directive';

describe('CompletedTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CompletedTaskDirective();
    expect(directive).toBeTruthy();
  });
});
